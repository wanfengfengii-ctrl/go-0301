# 种质库种子复苏活力验证与保存批次放行闭环

## 项目目标

建设面向种质库的种子复苏活力验证服务。工作人员将保存批次、母本来源、采集地、分装样本、库位环境、预处理方案、培养条件、观察日程和放行阈值锁定为不可变试验代次；系统随后控制样本领用、休眠解除、培养皿布置、周期观察、污染隔离、复测传播和双人复核。Go 后端以关系数据库持久化事件、快照、幂等记录、设备租约和终局凭据，重启后恢复活动代次、观察游标、待重试设备调用及租约。真实前端提供批次建档、样本谱系、培养矩阵、异常关联、复核和放行操作。生产代码要求至少20个Go文件、4至6个有业务意义的包，约2500有效行并限定在2000至3000行；规模不得依靠测试、前端、注释或重复填充。公开测试至少分布于4个测试文件，包含不少于12个确定性用例；服务及前端构建支持amd64与arm64容器。

## 端到端业务流程

1. 试验锁定：录入物种、母本来源、采集批次、保存批次、分装样本、含水率、库位环境摘要、预处理方案、培养条件、观察日程、抽样映射和阈值；校验身份、谱系、样本量及摘要后生成不可变试验代次。
2. 样本领用：在单一事务中将分装样本的整数粒数分配至培养组、复测留样、含水率测定、污染隔离或合理损耗，并同时获取培养箱、恒温水浴或成像设备的限时租约。
3. 预处理与培养：按方案记录回温、浸种、划伤、层积、播种和入箱证据；培养皿位置、处理阶段、环境范围、操作者和逻辑时间必须与锁定代次及有效租约一致。
4. 周期观察：按锁定日程记录发芽、硬实、腐烂、异常幼苗和未发芽粒数；设备拒绝、断连、超时或载荷错误仅形成确定序号的待重试调用，成功回执才能形成观察证据。
5. 异常复测：污染、环境越界、对照失效或活力不足按同母本来源、共享预处理批次、培养箱暴露窗口和相邻皿位生成排序稳定的唯一复测集合，并建立隔离的新代次。
6. 复核放行：完成粒数守恒、阶段闭合、日程覆盖、对照有效、活力计算和全部复测后，由两名不同且具备资格的人员独立签署；放行、隔离保存或作废通过单写终局屏障竞争唯一结果。

## 核心组件与职责

1. 试验规则目录：保存物种判定规则、预处理方案、环境区间、观察分类、抽样比例、活力阈值、复核资格及其版本摘要，并在锁定时形成不可变快照。
2. 批次谱系与粒数聚合：维护采集批次、保存批次、分装样本、培养组和培养皿的只追加谱系，执行单父、无环、身份唯一和整数粒数守恒。
3. 培养流程与资源租约引擎：实现预处理及培养阶段状态机，管理培养箱、水浴和成像设备的逻辑时钟互斥租约，拒绝越级、迟到或错误代次写入。
4. 观察证据与复测引擎：记录日程观察、环境证据、设备调用和定点活力计算；根据异常关联生成唯一复测集合，并隔离不同代次的证据。
5. 复核与终局仲裁器：验证所有前置条件、签署人独立性和资格快照，以数据库唯一约束生成永久终局凭据并支持事件重放恢复。
6. HTTP与前端交互层：提供稳定JSON接口、幂等命令和规范排序响应，并托管经package.json及锁文件确定构建的前端，展示谱系、培养矩阵、观察进度、复测依据与终局状态。

## 领域规则与不变量

1. 试验锁定后，样本全集、抽样映射、预处理方案、培养条件、观察日程和阈值不得原位修改；任何变化必须创建新代次，旧证据永久可查。
2. 谱系边只可追加且必须无环；每个保存批次、分装样本、培养组和培养皿仅有一个直接来源，身份在任务内唯一。
3. 所有样本量使用非负整数粒数；分装输入必须等于培养、留样、测定、隔离与合理损耗之和，失败事务不得留下部分分配。
4. 处理阶段严格为回温、休眠解除、播种、入箱、周期观察和结案；方案可声明不适用阶段，但锁定后形成的阶段序列必须保持连续前缀。
5. 写入设备相关证据必须持有资源、用途、操作者和代次均匹配的未过期租约；逻辑时间不得早于已有证据或锁定日程窗口。
6. 发芽率、污染率、异常苗率和活力指数采用显式比例尺的整数定点计算，使用半远离零舍入，并检查负值、除零和溢出。
7. 观察分类互斥且完备，同一培养皿每次累计分类数不得减少，总数不得超过播种粒数；结案时分类总数必须等于播种粒数。
8. 设备故障不得产生推测值；成功回执必须匹配请求摘要、当前代次和重试序号，旧代次或已结案调用只能作为历史保留。
9. 同一异常来源在同一代次仅生成一个规范化复测集合；成员按保存批次、分装样本、培养组、皿位排序，新代次不得吸收旧代次迟到证据。
10. 幂等键首次绑定规范化命令摘要；同内容重试返回原结果，不同内容返回稳定冲突。三类终局请求共享唯一槽位。

## 数据模型与持久化

1. RuleSnapshot：物种规则、方案阶段、环境区间、观察日程、定点比例尺、阈值、资格规则、版本及摘要。
2. ViabilityTrial与Generation：试验编号、锁定状态、当前代次、输入摘要、逻辑时间、恢复版本和终局状态。
3. SeedLot、SampleUnit与LineageEvent：采集批次、保存批次、分装样本、培养组、培养皿及只追加来源边、整数粒数和库位摘要。
4. TreatmentEvent与ResourceLease：阶段、方案、操作者、逻辑时间、证据摘要，以及资源、持有者、用途、代次、到期时间和并发版本。
5. Observation、EnvironmentEvidence与InstrumentCall：皿位分类计数、环境读数、请求摘要、重试序号、故障类型或成功载荷。
6. RetestSet：异常来源、关联依据、规范成员、来源代次、目标代次、集合摘要和完成状态。
7. Review与TerminalCredential：签署人、资格快照、签署摘要、终局类型、凭据编号和提交逻辑时间。
8. IdempotencyRecord与ProjectionCheckpoint：幂等键、命令摘要、原始结果，以及事件重放位置、投影摘要和恢复状态。

## 公开接口

1. POST /api/trials 与 POST /api/trials/{id}/lock：创建并锁定试验；GET /api/trials/{id}：读取当前代次和缺失条件。
2. POST /api/trials/{id}/samples/allocate：原子分配样本；GET /api/trials/{id}/lineage：按规范顺序返回谱系和粒数守恒结果。
3. POST /api/leases/acquire、/renew、/release：管理资源租约；冲突响应包含资源编号和确定的到期逻辑时间。
4. POST /api/trials/{id}/plates/{plateId}/events：提交处理阶段；POST /api/trials/{id}/observations：提交周期观察。
5. POST /api/instrument-calls 与 POST /api/instrument-calls/{id}/receipts：创建设备调用并提交故障或成功回执。
6. POST /api/trials/{id}/retests 与 GET /api/trials/{id}/retests/{generation}：生成并查询复测集合及传播依据。
7. POST /api/trials/{id}/reviews、POST /api/trials/{id}/terminal、GET /api/trials/{id}/credential：完成复核、竞争终局并读取凭据。
8. GET /：提供可构建前端；页面支持试验锁定、样本分配、培养阶段录入、观察矩阵、租约与重试查询、复测确认和双人终局操作，并通过上述API实时读写。

## 失败边界

1. 输入错误使用稳定代码，包括STALE_RULE_DIGEST、DUPLICATE_SAMPLE_ID、LINEAGE_CYCLE、MULTIPLE_PARENT、INVALID_SAMPLE_COUNT、MISSING_CONTROL和INVALID_SCHEDULE。
2. 流程与并发错误包括SAMPLE_ALREADY_ALLOCATED、LEASE_CONFLICT、LEASE_EXPIRED、STAGE_GAP、GENERATION_MISMATCH、TIME_REGRESSION、OBSERVATION_REGRESSION和TERMINAL_ALREADY_DECIDED。
3. 算术层在事务写入前返回NEGATIVE_MEASURE、DIVIDE_BY_ZERO、ARITHMETIC_OVERFLOW或INVALID_FIXED_POINT_SCALE。
4. 设备拒绝、断连、超时和载荷错误分别持久化为INSTRUMENT_REJECTED、INSTRUMENT_DISCONNECTED、INSTRUMENT_TIMEOUT和MALFORMED_RECEIPT，不推进观察覆盖。
5. 事务必须覆盖谱系追加、粒数分配、租约获取、阶段事件和终局凭据；唯一约束或版本竞争失败时整体回滚。
6. 批量错误和复测成员采用固定字段排序，不受映射遍历、协程完成顺序、处理器架构或重启影响。
7. 恢复时重放持久事件并校验摘要链；摘要断裂使试验进入只读故障态，系统不得补造阶段、读数或凭据。

## 验收标准

1. 锁定完整固定样本全集、来源谱系、抽样映射、方案、环境、日程、阈值和代次，并稳定拒绝重复身份、来源错配及陈旧摘要。
2. 整数粒数在保存批次、分装样本和全部去向间严格守恒；并发领用仅一个成功，幂等重试返回原结果，失败无部分写入。
3. 每个培养皿形成锁定方案规定的连续阶段前缀；越级、错误代次、时间倒退或过期租约均不产生有效证据。
4. 观察分类满足互斥、单调和总量边界；活力指标以确定性整数定点算法计算，跨架构及重启结果一致。
5. 脚本化设备拒绝、断连、超时和载荷错误只产生确定重试记录，匹配当前代次的成功回执才更新覆盖状态。
6. 异常按锁定关联规则生成唯一且有序的复测集合；新代次保留完整历史，并拒绝旧代次迟到证据影响当前结论。
7. 满足守恒、阶段、观察、对照、阈值、复测及双人独立复核后才可产生终局凭据；并发终局请求仅一个胜出。
8. 关闭并重启数据库后可恢复当前代次、阶段前缀、租约、待重试调用、复测集合和终局凭据；前端可完成一条真实端到端流程。

## 确定性测试场景

1. lock_lineage_test.go：锁定虚构种质数据并断言样本全集、方案、日程和规则摘要固定。
2. lock_lineage_test.go：构造谱系回边与双父来源，分别断言LINEAGE_CYCLE和MULTIPLE_PARENT且无残留事件。
3. lock_lineage_test.go：提交重复样本身份和陈旧规则摘要，断言稳定错误及排序明细。
4. allocation_lease_test.go：两协程经同步屏障竞争同一分装样本，断言仅一方完成分配。
5. allocation_lease_test.go：验证培养、留样、测定、隔离及损耗粒数守恒，并断言故障注入时整体回滚。
6. allocation_lease_test.go：竞争同一培养箱租约，推进逻辑时钟后验证旧持有者失效及新持有者成功获取。
7. treatment_observation_test.go：按锁定阶段推进培养皿，断言每步连续；跳过播种直接入箱返回STAGE_GAP。
8. treatment_observation_test.go：提交分类计数回退、分类总数超限及时间倒退，断言有效观察数不变。
9. treatment_observation_test.go：验证发芽率和活力指数的舍入边界、除零及溢出。
10. instrument_retest_test.go：脚本化设备拒绝、断连、超时和错误载荷，断言重试序号固定且覆盖率不变。
11. instrument_retest_test.go：制造共享处理批次、暴露窗口和相邻皿位异常，精确断言复测成员及顺序。
12. instrument_retest_test.go：创建新代次后提交旧代次成功回执，断言历史保留而当前状态不变。
13. terminal_recovery_http_test.go：相同幂等键同内容返回原结果，不同内容返回IDEMPOTENCY_CONFLICT。
14. terminal_recovery_http_test.go：双人签署后并发提交三类终局请求，断言仅一个凭据生成。
15. terminal_recovery_http_test.go：重启数据库并断言阶段、租约、重试、复测和终局状态完整恢复。
16. terminal_recovery_http_test.go：通过HTTP完成锁定、分配、培养、观察、复核及终局流程，并断言前端入口和静态资源可加载。

## 组件追踪关系

1. 试验规则目录支撑试验锁定、RuleSnapshot、锁定接口以及验收1和4。
2. 批次谱系与粒数聚合支撑样本领用、SeedLot、SampleUnit、LineageEvent以及验收1和2。
3. 培养流程与资源租约引擎支撑预处理培养、TreatmentEvent、ResourceLease以及验收2和3。
4. 观察证据与复测引擎支撑周期观察和异常复测、Observation、InstrumentCall、RetestSet以及验收4至6。
5. 复核与终局仲裁器支撑双人复核和终局竞争、Review、TerminalCredential以及验收7和8。
6. HTTP与前端交互层贯穿全部流程，负责幂等入口、稳定错误、确定排序和真实页面交互，并由端到端及恢复测试覆盖。

## 独特性

项目以长期低温保存种子的复苏活力判定为唯一主线，将整数粒数守恒、培养皿阶段前缀、周期分类单调性、培养暴露关联复测和双人放行凭据组合为可恢复的证据闭环；核心状态均围绕种质样本、休眠处理、发芽观察与保存决策展开。
